// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file SustainMLServiceServer.cxx
 * Server implementation for interfaces
 *
 * This file was generated by the tool fastddsgen (version: 4.1.0).
 */

#include "SustainMLServiceServer.hpp"

#include <atomic>
#include <condition_variable>
#include <map>
#include <memory>
#include <mutex>
#include <queue>
#include <stdexcept>
#include <string>
#include <thread>
#include <vector>

#include <fastdds/dds/builtin/topic/PublicationBuiltinTopicData.hpp>
#include <fastdds/dds/core/condition/Condition.hpp>
#include <fastdds/dds/core/condition/GuardCondition.hpp>
#include <fastdds/dds/core/condition/WaitSet.hpp>
#include <fastdds/dds/core/Time_t.hpp>
#include <fastdds/dds/domain/DomainParticipant.hpp>
#include <fastdds/dds/domain/qos/ReplierQos.hpp>
#include <fastdds/dds/log/Log.hpp>
#include <fastdds/dds/rpc/exceptions.hpp>
#include <fastdds/dds/rpc/interfaces.hpp>
#include <fastdds/dds/rpc/RequestInfo.hpp>
#include <fastdds/dds/rpc/Replier.hpp>
#include <fastdds/dds/rpc/Service.hpp>
#include <fastdds/dds/rpc/ServiceTypeSupport.hpp>
#include <fastdds/dds/subscriber/DataReader.hpp>

#include "SustainMLService.hpp"
#include "SustainMLService_details.hpp"
#include "SustainMLServicePubSubTypes.hpp"

//{ interface AppRequirementsService

namespace detail {

namespace fdds = eprosima::fastdds::dds;
namespace frpc = eprosima::fastdds::dds::rpc;
namespace frtps = eprosima::fastdds::rtps;

class AppRequirementsServiceServerLogic
    : public frpc::RpcServer
    , public std::enable_shared_from_this<AppRequirementsServiceServerLogic>
{
    using RequestType = AppRequirementsService_Request;
    using ReplyType = AppRequirementsService_Reply;

public:

    AppRequirementsServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            size_t thread_pool_size,
            std::shared_ptr<AppRequirementsServiceServer_IServerImplementation> implementation)
        : AppRequirementsServiceServerLogic(
            part,
            service_name,
            qos,
            std::make_shared<ThreadPool>(*this, thread_pool_size),
            std::move(implementation))
    {
    }

    AppRequirementsServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            std::shared_ptr<frpc::RpcServerSchedulingStrategy> scheduler,
            std::shared_ptr<AppRequirementsServiceServer_IServerImplementation> implementation)
        : frpc::RpcServer()
        , participant_(part)
        , request_scheduler_(scheduler)
        , implementation_(std::move(implementation))
    {
        // Register the service type support
        auto service_type = create_AppRequirementsService_service_type_support();
        auto ret = service_type.register_service_type(&participant_, "AppRequirementsService");
        if (ret != fdds::RETCODE_OK)
        {
            throw std::runtime_error("Error registering service type");
        }

        // Create the service
        service_ = participant_.create_service(service_name, "AppRequirementsService");
        if (nullptr == service_)
        {
            throw std::runtime_error("Error creating service");
        }

        // Create the replier
        replier_ = participant_.create_service_replier(service_, qos);
        if (nullptr == replier_)
        {
            throw std::runtime_error("Error creating requester");
        }
    }

    ~AppRequirementsServiceServerLogic() override
    {
        if (nullptr != replier_)
        {
            participant_.delete_service_replier(service_->get_service_name(), replier_);
        }

        if (nullptr != service_)
        {
            participant_.delete_service(service_);
        }
    }

    void run() override
    {
        finish_condition_.set_trigger_value(false);
        fdds::WaitSet waitset;
        waitset.attach_condition(finish_condition_);
        fdds::StatusCondition& status_condition = replier_->get_replier_reader()->get_statuscondition();
        status_condition.set_enabled_statuses(fdds::StatusMask::data_available());
        waitset.attach_condition(status_condition);

        while (true)
        {
            fdds::ConditionSeq active_conditions;
            waitset.wait(active_conditions, fdds::c_TimeInfinite);

            // Early exit if the finish condition is triggered
            if (finish_condition_.get_trigger_value())
            {
                break;
            }

            // Take and process the requests
            auto ctx = std::make_shared<RequestContext>();
            while (fdds::RETCODE_OK == ctx->take_from(replier_))
            {
                if (ctx->validate_request())
                {
                    process_request(ctx);
                }
                else
                {
                    ctx->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier_);
                }

                // Prepare next request context
                ctx = std::make_shared<RequestContext>();
            }
        }
    }

    void stop() override
    {
        // Notify all threads to finish
        finish_condition_.set_trigger_value(true);

        // Cancel all pending requests
        {
            std::lock_guard<std::mutex> _(mtx_);
            for (auto& it : processing_requests_)
            {
                it.second->cancel();
            }
            processing_requests_.clear();
        }

        // Wait for all threads to finish
        request_scheduler_->server_stopped(shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        auto ctx = std::dynamic_pointer_cast<RequestContext>(request);
        if (ctx)
        {
            execute_request(ctx);
        }
        else
        {
            throw std::runtime_error("Invalid request context type");
        }
    }

private:

    //{ Output feed helpers

    struct IOutputFeedCancellator
    {
        virtual ~IOutputFeedCancellator() = default;
        virtual void cancel() = 0;
    };

    //} Output feed helpers

    //{ Input feed helpers

    struct IInputFeedProcessor
    {
        virtual ~IInputFeedProcessor() = default;
        virtual bool process_additional_request(
                const RequestType& request) = 0;
        virtual void cancel_input_feed() = 0;
    };

    //} Input feed helpers

    //{ operation update_configuration

    //} operation update_configuration

    struct RequestContext : frpc::RpcRequest
    {
        RequestType request;
        frpc::RequestInfo info;
        frtps::GUID_t client_id;
        fdds::PublicationBuiltinTopicData pub_data;

        struct update_configuration_feeds_t
        {
        }
        update_configuration_feeds;

        const frtps::GUID_t& get_client_id() const override
        {
            return client_id;
        }

        const frtps::RemoteLocatorList& get_client_locators() const override
        {
            return pub_data.remote_locators;
        }

        fdds::ReturnCode_t take_from(
                frpc::Replier* replier)
        {
            fdds::ReturnCode_t ret = replier->take_request(&request, info);
            if (ret == fdds::RETCODE_OK)
            {
                client_id = info.sample_identity.writer_guid();
                ret = replier->get_replier_reader()->get_matched_publication_data(pub_data, client_id);
            }
            return ret;
        }

        bool validate_request() const
        {
            size_t n_fields = 0;
            n_fields += request.update_configuration.has_value() ? 1 : 0;

            return n_fields == 1U;
        }

        void process_additional_request(
                const std::shared_ptr<RequestContext>& ctx,
                frpc::Replier* replier,
                bool& should_erase)
        {
            should_erase = false;
            if (ctx->info.related_sample_identity == info.related_sample_identity)
            {
                // Pass request to input feed processors
                should_erase = true;
                for (const auto& input_feed : input_feed_processors_)
                {
                    if (input_feed->process_additional_request(ctx->request))
                    {
                        should_erase = false;
                        break;
                    }
                }

                // If no input feed processor handled the request, send an exception
                if (should_erase)
                {
                    send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier);
                }
            }
            else
            {
                // This is not the expected request
                should_erase = true;
            }
        }

        bool prepare(
                frpc::Replier* replier)
        {
            if (request.update_configuration.has_value())
            {
                return prepare_update_configuration(replier);
            }


            send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_OPERATION, replier);
            return false;
        }

        void send_exception(
                frpc::RemoteExceptionCode_t ex,
                frpc::Replier* replier)
        {
            ReplyType reply{};
            reply.remoteEx = ex;
            replier->send_reply(&reply, info);
        }

        void cancel()
        {
            // Cancel output feed
            if (output_feed_cancellator_)
            {
                output_feed_cancellator_->cancel();
            }

            // Cancel input feeds
            for (const auto& input_feed : input_feed_processors_)
            {
                input_feed->cancel_input_feed();
            }
        }

    private:

        std::shared_ptr<IOutputFeedCancellator> output_feed_cancellator_;
        std::vector<std::shared_ptr<IInputFeedProcessor>> input_feed_processors_;

        bool prepare_update_configuration(
                frpc::Replier* replier)
        {
            static_cast<void>(replier);
            return true;
        }

    };

    struct ThreadPool
        : public frpc::RpcServerSchedulingStrategy
    {
        ThreadPool(
                AppRequirementsServiceServerLogic& server,
                size_t num_threads)
            : server_(server)
        {
            // Create worker threads (at least one)
            if (num_threads == 0)
            {
                num_threads = 1;
            }

            auto process_requests = [this]()
                    {
                        while (!finished_)
                        {
                            std::shared_ptr<frpc::RpcRequest> req;
                            {
                                std::unique_lock<std::mutex> lock(mtx_);
                                cv_.wait(lock, [this]()
                                        {
                                            return finished_ || !requests_.empty();
                                        });
                                if (finished_)
                                {
                                    break;
                                }
                                req = requests_.front();
                                requests_.pop();
                            }

                            // Process the request
                            server_.execute_request(req);
                        }
                    };

            for (size_t i = 0; i < num_threads; ++i)
            {
                threads_.emplace_back(process_requests);
            }
        }

        void schedule_request(
                const std::shared_ptr<frpc::RpcRequest>& req,
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            std::lock_guard<std::mutex> lock(mtx_);
            if (!finished_)
            {
                requests_.push(req);
                cv_.notify_one();
            }
        }

        void server_stopped(
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            // Notify all threads in the pool to stop
            {
                std::lock_guard<std::mutex> lock(mtx_);
                finished_ = true;
                cv_.notify_all();
            }

            // Wait for all threads to finish
            for (auto& thread : threads_)
            {
                if (thread.joinable())
                {
                    thread.join();
                }
            }
            threads_.clear();
        }

        AppRequirementsServiceServerLogic& server_;
        std::mutex mtx_;
        std::condition_variable cv_;
        std::queue<std::shared_ptr<frpc::RpcRequest>> requests_;
        bool finished_{ false };
        std::vector<std::thread> threads_;
    };

    void process_request(
            const std::shared_ptr<RequestContext>& ctx)
    {
        auto id = ctx->info.related_sample_identity;

        {
            std::lock_guard<std::mutex> _(mtx_);
            auto it = processing_requests_.find(id);
            if (it != processing_requests_.end())
            {
                bool should_erase = false;
                it->second->process_additional_request(ctx, replier_, should_erase);
                if (should_erase)
                {
                    processing_requests_.erase(it);
                }
                return;
            }

            if (!ctx->prepare(replier_))
            {
                return;
            }

            processing_requests_[id] = ctx;
        }

        request_scheduler_->schedule_request(ctx, shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<RequestContext>& req)
    {
        try
        {
            for (;;)
            {
                if (req->request.update_configuration.has_value())
                {
                    try
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::AppRequirementsService_update_configuration_Result{};
                        reply.update_configuration->result = detail::AppRequirementsService_update_configuration_Out{};
                        reply.update_configuration->result->return_ = implementation_->update_configuration(
                            *req,
                            req->request.update_configuration->configuration);
                        replier_->send_reply(&reply, req->info);
                    }
                    catch (const InternalError& ex)
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::AppRequirementsService_update_configuration_Result{};
                        reply.update_configuration->InternalError_ex = ex;
                        replier_->send_reply(&reply, req->info);
                    }
                    break;
                }

                req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNSUPPORTED, replier_);
                break;
            }
        }
        catch (const frpc::RpcRemoteException& ex)
        {
            req->send_exception(ex.code(), replier_);
        }
        catch (...)
        {
            req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_EXCEPTION, replier_);
        }

        {
            std::lock_guard<std::mutex> _(mtx_);
            processing_requests_.erase(req->info.related_sample_identity);
        }
    }

    fdds::DomainParticipant& participant_;
    frpc::Service* service_ = nullptr;
    frpc::Replier* replier_ = nullptr;
    fdds::GuardCondition finish_condition_;
    std::mutex mtx_;
    std::map<frtps::SampleIdentity, std::shared_ptr<RequestContext>> processing_requests_;
    std::shared_ptr<frpc::RpcServerSchedulingStrategy> request_scheduler_;
    std::shared_ptr<AppRequirementsServiceServer_IServerImplementation> implementation_;

};

struct AppRequirementsServiceServerProxy
    : public frpc::RpcServer
{
    AppRequirementsServiceServerProxy(
            std::shared_ptr<frpc::RpcServer> impl)
        : impl_(std::move(impl))
    {
    }

    ~AppRequirementsServiceServerProxy() override
    {
        if (impl_)
        {
            impl_->stop();
        }
    }

    void run() override
    {
        impl_->run();
    }

    void stop() override
    {
        impl_->stop();
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        impl_->execute_request(request);
    }

private:

    std::shared_ptr<frpc::RpcServer> impl_;
};

}  // namespace detail

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_AppRequirementsServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        size_t thread_pool_size,
        std::shared_ptr<AppRequirementsServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::AppRequirementsServiceServerLogic>(
        part, service_name, qos, thread_pool_size, implementation);
    return std::make_shared<detail::AppRequirementsServiceServerProxy>(ptr);
}

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_AppRequirementsServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServerSchedulingStrategy> scheduler,
        std::shared_ptr<AppRequirementsServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::AppRequirementsServiceServerLogic>(
        part, service_name, qos, scheduler, implementation);
    return std::make_shared<detail::AppRequirementsServiceServerProxy>(ptr);
}

//} interface AppRequirementsService

//{ interface HWConstraintsService

namespace detail {

namespace fdds = eprosima::fastdds::dds;
namespace frpc = eprosima::fastdds::dds::rpc;
namespace frtps = eprosima::fastdds::rtps;

class HWConstraintsServiceServerLogic
    : public frpc::RpcServer
    , public std::enable_shared_from_this<HWConstraintsServiceServerLogic>
{
    using RequestType = HWConstraintsService_Request;
    using ReplyType = HWConstraintsService_Reply;

public:

    HWConstraintsServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            size_t thread_pool_size,
            std::shared_ptr<HWConstraintsServiceServer_IServerImplementation> implementation)
        : HWConstraintsServiceServerLogic(
            part,
            service_name,
            qos,
            std::make_shared<ThreadPool>(*this, thread_pool_size),
            std::move(implementation))
    {
    }

    HWConstraintsServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            std::shared_ptr<frpc::RpcServerSchedulingStrategy> scheduler,
            std::shared_ptr<HWConstraintsServiceServer_IServerImplementation> implementation)
        : frpc::RpcServer()
        , participant_(part)
        , request_scheduler_(scheduler)
        , implementation_(std::move(implementation))
    {
        // Register the service type support
        auto service_type = create_HWConstraintsService_service_type_support();
        auto ret = service_type.register_service_type(&participant_, "HWConstraintsService");
        if (ret != fdds::RETCODE_OK)
        {
            throw std::runtime_error("Error registering service type");
        }

        // Create the service
        service_ = participant_.create_service(service_name, "HWConstraintsService");
        if (nullptr == service_)
        {
            throw std::runtime_error("Error creating service");
        }

        // Create the replier
        replier_ = participant_.create_service_replier(service_, qos);
        if (nullptr == replier_)
        {
            throw std::runtime_error("Error creating requester");
        }
    }

    ~HWConstraintsServiceServerLogic() override
    {
        if (nullptr != replier_)
        {
            participant_.delete_service_replier(service_->get_service_name(), replier_);
        }

        if (nullptr != service_)
        {
            participant_.delete_service(service_);
        }
    }

    void run() override
    {
        finish_condition_.set_trigger_value(false);
        fdds::WaitSet waitset;
        waitset.attach_condition(finish_condition_);
        fdds::StatusCondition& status_condition = replier_->get_replier_reader()->get_statuscondition();
        status_condition.set_enabled_statuses(fdds::StatusMask::data_available());
        waitset.attach_condition(status_condition);

        while (true)
        {
            fdds::ConditionSeq active_conditions;
            waitset.wait(active_conditions, fdds::c_TimeInfinite);

            // Early exit if the finish condition is triggered
            if (finish_condition_.get_trigger_value())
            {
                break;
            }

            // Take and process the requests
            auto ctx = std::make_shared<RequestContext>();
            while (fdds::RETCODE_OK == ctx->take_from(replier_))
            {
                if (ctx->validate_request())
                {
                    process_request(ctx);
                }
                else
                {
                    ctx->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier_);
                }

                // Prepare next request context
                ctx = std::make_shared<RequestContext>();
            }
        }
    }

    void stop() override
    {
        // Notify all threads to finish
        finish_condition_.set_trigger_value(true);

        // Cancel all pending requests
        {
            std::lock_guard<std::mutex> _(mtx_);
            for (auto& it : processing_requests_)
            {
                it.second->cancel();
            }
            processing_requests_.clear();
        }

        // Wait for all threads to finish
        request_scheduler_->server_stopped(shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        auto ctx = std::dynamic_pointer_cast<RequestContext>(request);
        if (ctx)
        {
            execute_request(ctx);
        }
        else
        {
            throw std::runtime_error("Invalid request context type");
        }
    }

private:

    //{ Output feed helpers

    struct IOutputFeedCancellator
    {
        virtual ~IOutputFeedCancellator() = default;
        virtual void cancel() = 0;
    };

    //} Output feed helpers

    //{ Input feed helpers

    struct IInputFeedProcessor
    {
        virtual ~IInputFeedProcessor() = default;
        virtual bool process_additional_request(
                const RequestType& request) = 0;
        virtual void cancel_input_feed() = 0;
    };

    //} Input feed helpers

    //{ operation update_configuration

    //} operation update_configuration

    struct RequestContext : frpc::RpcRequest
    {
        RequestType request;
        frpc::RequestInfo info;
        frtps::GUID_t client_id;
        fdds::PublicationBuiltinTopicData pub_data;

        struct update_configuration_feeds_t
        {
        }
        update_configuration_feeds;

        const frtps::GUID_t& get_client_id() const override
        {
            return client_id;
        }

        const frtps::RemoteLocatorList& get_client_locators() const override
        {
            return pub_data.remote_locators;
        }

        fdds::ReturnCode_t take_from(
                frpc::Replier* replier)
        {
            fdds::ReturnCode_t ret = replier->take_request(&request, info);
            if (ret == fdds::RETCODE_OK)
            {
                client_id = info.sample_identity.writer_guid();
                ret = replier->get_replier_reader()->get_matched_publication_data(pub_data, client_id);
            }
            return ret;
        }

        bool validate_request() const
        {
            size_t n_fields = 0;
            n_fields += request.update_configuration.has_value() ? 1 : 0;

            return n_fields == 1U;
        }

        void process_additional_request(
                const std::shared_ptr<RequestContext>& ctx,
                frpc::Replier* replier,
                bool& should_erase)
        {
            should_erase = false;
            if (ctx->info.related_sample_identity == info.related_sample_identity)
            {
                // Pass request to input feed processors
                should_erase = true;
                for (const auto& input_feed : input_feed_processors_)
                {
                    if (input_feed->process_additional_request(ctx->request))
                    {
                        should_erase = false;
                        break;
                    }
                }

                // If no input feed processor handled the request, send an exception
                if (should_erase)
                {
                    send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier);
                }
            }
            else
            {
                // This is not the expected request
                should_erase = true;
            }
        }

        bool prepare(
                frpc::Replier* replier)
        {
            if (request.update_configuration.has_value())
            {
                return prepare_update_configuration(replier);
            }


            send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_OPERATION, replier);
            return false;
        }

        void send_exception(
                frpc::RemoteExceptionCode_t ex,
                frpc::Replier* replier)
        {
            ReplyType reply{};
            reply.remoteEx = ex;
            replier->send_reply(&reply, info);
        }

        void cancel()
        {
            // Cancel output feed
            if (output_feed_cancellator_)
            {
                output_feed_cancellator_->cancel();
            }

            // Cancel input feeds
            for (const auto& input_feed : input_feed_processors_)
            {
                input_feed->cancel_input_feed();
            }
        }

    private:

        std::shared_ptr<IOutputFeedCancellator> output_feed_cancellator_;
        std::vector<std::shared_ptr<IInputFeedProcessor>> input_feed_processors_;

        bool prepare_update_configuration(
                frpc::Replier* replier)
        {
            static_cast<void>(replier);
            return true;
        }

    };

    struct ThreadPool
        : public frpc::RpcServerSchedulingStrategy
    {
        ThreadPool(
                HWConstraintsServiceServerLogic& server,
                size_t num_threads)
            : server_(server)
        {
            // Create worker threads (at least one)
            if (num_threads == 0)
            {
                num_threads = 1;
            }

            auto process_requests = [this]()
                    {
                        while (!finished_)
                        {
                            std::shared_ptr<frpc::RpcRequest> req;
                            {
                                std::unique_lock<std::mutex> lock(mtx_);
                                cv_.wait(lock, [this]()
                                        {
                                            return finished_ || !requests_.empty();
                                        });
                                if (finished_)
                                {
                                    break;
                                }
                                req = requests_.front();
                                requests_.pop();
                            }

                            // Process the request
                            server_.execute_request(req);
                        }
                    };

            for (size_t i = 0; i < num_threads; ++i)
            {
                threads_.emplace_back(process_requests);
            }
        }

        void schedule_request(
                const std::shared_ptr<frpc::RpcRequest>& req,
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            std::lock_guard<std::mutex> lock(mtx_);
            if (!finished_)
            {
                requests_.push(req);
                cv_.notify_one();
            }
        }

        void server_stopped(
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            // Notify all threads in the pool to stop
            {
                std::lock_guard<std::mutex> lock(mtx_);
                finished_ = true;
                cv_.notify_all();
            }

            // Wait for all threads to finish
            for (auto& thread : threads_)
            {
                if (thread.joinable())
                {
                    thread.join();
                }
            }
            threads_.clear();
        }

        HWConstraintsServiceServerLogic& server_;
        std::mutex mtx_;
        std::condition_variable cv_;
        std::queue<std::shared_ptr<frpc::RpcRequest>> requests_;
        bool finished_{ false };
        std::vector<std::thread> threads_;
    };

    void process_request(
            const std::shared_ptr<RequestContext>& ctx)
    {
        auto id = ctx->info.related_sample_identity;

        {
            std::lock_guard<std::mutex> _(mtx_);
            auto it = processing_requests_.find(id);
            if (it != processing_requests_.end())
            {
                bool should_erase = false;
                it->second->process_additional_request(ctx, replier_, should_erase);
                if (should_erase)
                {
                    processing_requests_.erase(it);
                }
                return;
            }

            if (!ctx->prepare(replier_))
            {
                return;
            }

            processing_requests_[id] = ctx;
        }

        request_scheduler_->schedule_request(ctx, shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<RequestContext>& req)
    {
        try
        {
            for (;;)
            {
                if (req->request.update_configuration.has_value())
                {
                    try
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::HWConstraintsService_update_configuration_Result{};
                        reply.update_configuration->result = detail::HWConstraintsService_update_configuration_Out{};
                        reply.update_configuration->result->return_ = implementation_->update_configuration(
                            *req,
                            req->request.update_configuration->configuration);
                        replier_->send_reply(&reply, req->info);
                    }
                    catch (const InternalError& ex)
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::HWConstraintsService_update_configuration_Result{};
                        reply.update_configuration->InternalError_ex = ex;
                        replier_->send_reply(&reply, req->info);
                    }
                    break;
                }

                req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNSUPPORTED, replier_);
                break;
            }
        }
        catch (const frpc::RpcRemoteException& ex)
        {
            req->send_exception(ex.code(), replier_);
        }
        catch (...)
        {
            req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_EXCEPTION, replier_);
        }

        {
            std::lock_guard<std::mutex> _(mtx_);
            processing_requests_.erase(req->info.related_sample_identity);
        }
    }

    fdds::DomainParticipant& participant_;
    frpc::Service* service_ = nullptr;
    frpc::Replier* replier_ = nullptr;
    fdds::GuardCondition finish_condition_;
    std::mutex mtx_;
    std::map<frtps::SampleIdentity, std::shared_ptr<RequestContext>> processing_requests_;
    std::shared_ptr<frpc::RpcServerSchedulingStrategy> request_scheduler_;
    std::shared_ptr<HWConstraintsServiceServer_IServerImplementation> implementation_;

};

struct HWConstraintsServiceServerProxy
    : public frpc::RpcServer
{
    HWConstraintsServiceServerProxy(
            std::shared_ptr<frpc::RpcServer> impl)
        : impl_(std::move(impl))
    {
    }

    ~HWConstraintsServiceServerProxy() override
    {
        if (impl_)
        {
            impl_->stop();
        }
    }

    void run() override
    {
        impl_->run();
    }

    void stop() override
    {
        impl_->stop();
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        impl_->execute_request(request);
    }

private:

    std::shared_ptr<frpc::RpcServer> impl_;
};

}  // namespace detail

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_HWConstraintsServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        size_t thread_pool_size,
        std::shared_ptr<HWConstraintsServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::HWConstraintsServiceServerLogic>(
        part, service_name, qos, thread_pool_size, implementation);
    return std::make_shared<detail::HWConstraintsServiceServerProxy>(ptr);
}

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_HWConstraintsServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServerSchedulingStrategy> scheduler,
        std::shared_ptr<HWConstraintsServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::HWConstraintsServiceServerLogic>(
        part, service_name, qos, scheduler, implementation);
    return std::make_shared<detail::HWConstraintsServiceServerProxy>(ptr);
}

//} interface HWConstraintsService

//{ interface HWResourcesService

namespace detail {

namespace fdds = eprosima::fastdds::dds;
namespace frpc = eprosima::fastdds::dds::rpc;
namespace frtps = eprosima::fastdds::rtps;

class HWResourcesServiceServerLogic
    : public frpc::RpcServer
    , public std::enable_shared_from_this<HWResourcesServiceServerLogic>
{
    using RequestType = HWResourcesService_Request;
    using ReplyType = HWResourcesService_Reply;

public:

    HWResourcesServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            size_t thread_pool_size,
            std::shared_ptr<HWResourcesServiceServer_IServerImplementation> implementation)
        : HWResourcesServiceServerLogic(
            part,
            service_name,
            qos,
            std::make_shared<ThreadPool>(*this, thread_pool_size),
            std::move(implementation))
    {
    }

    HWResourcesServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            std::shared_ptr<frpc::RpcServerSchedulingStrategy> scheduler,
            std::shared_ptr<HWResourcesServiceServer_IServerImplementation> implementation)
        : frpc::RpcServer()
        , participant_(part)
        , request_scheduler_(scheduler)
        , implementation_(std::move(implementation))
    {
        // Register the service type support
        auto service_type = create_HWResourcesService_service_type_support();
        auto ret = service_type.register_service_type(&participant_, "HWResourcesService");
        if (ret != fdds::RETCODE_OK)
        {
            throw std::runtime_error("Error registering service type");
        }

        // Create the service
        service_ = participant_.create_service(service_name, "HWResourcesService");
        if (nullptr == service_)
        {
            throw std::runtime_error("Error creating service");
        }

        // Create the replier
        replier_ = participant_.create_service_replier(service_, qos);
        if (nullptr == replier_)
        {
            throw std::runtime_error("Error creating requester");
        }
    }

    ~HWResourcesServiceServerLogic() override
    {
        if (nullptr != replier_)
        {
            participant_.delete_service_replier(service_->get_service_name(), replier_);
        }

        if (nullptr != service_)
        {
            participant_.delete_service(service_);
        }
    }

    void run() override
    {
        finish_condition_.set_trigger_value(false);
        fdds::WaitSet waitset;
        waitset.attach_condition(finish_condition_);
        fdds::StatusCondition& status_condition = replier_->get_replier_reader()->get_statuscondition();
        status_condition.set_enabled_statuses(fdds::StatusMask::data_available());
        waitset.attach_condition(status_condition);

        while (true)
        {
            fdds::ConditionSeq active_conditions;
            waitset.wait(active_conditions, fdds::c_TimeInfinite);

            // Early exit if the finish condition is triggered
            if (finish_condition_.get_trigger_value())
            {
                break;
            }

            // Take and process the requests
            auto ctx = std::make_shared<RequestContext>();
            while (fdds::RETCODE_OK == ctx->take_from(replier_))
            {
                if (ctx->validate_request())
                {
                    process_request(ctx);
                }
                else
                {
                    ctx->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier_);
                }

                // Prepare next request context
                ctx = std::make_shared<RequestContext>();
            }
        }
    }

    void stop() override
    {
        // Notify all threads to finish
        finish_condition_.set_trigger_value(true);

        // Cancel all pending requests
        {
            std::lock_guard<std::mutex> _(mtx_);
            for (auto& it : processing_requests_)
            {
                it.second->cancel();
            }
            processing_requests_.clear();
        }

        // Wait for all threads to finish
        request_scheduler_->server_stopped(shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        auto ctx = std::dynamic_pointer_cast<RequestContext>(request);
        if (ctx)
        {
            execute_request(ctx);
        }
        else
        {
            throw std::runtime_error("Invalid request context type");
        }
    }

private:

    //{ Output feed helpers

    struct IOutputFeedCancellator
    {
        virtual ~IOutputFeedCancellator() = default;
        virtual void cancel() = 0;
    };

    //} Output feed helpers

    //{ Input feed helpers

    struct IInputFeedProcessor
    {
        virtual ~IInputFeedProcessor() = default;
        virtual bool process_additional_request(
                const RequestType& request) = 0;
        virtual void cancel_input_feed() = 0;
    };

    //} Input feed helpers

    //{ operation update_configuration

    //} operation update_configuration

    struct RequestContext : frpc::RpcRequest
    {
        RequestType request;
        frpc::RequestInfo info;
        frtps::GUID_t client_id;
        fdds::PublicationBuiltinTopicData pub_data;

        struct update_configuration_feeds_t
        {
        }
        update_configuration_feeds;

        const frtps::GUID_t& get_client_id() const override
        {
            return client_id;
        }

        const frtps::RemoteLocatorList& get_client_locators() const override
        {
            return pub_data.remote_locators;
        }

        fdds::ReturnCode_t take_from(
                frpc::Replier* replier)
        {
            fdds::ReturnCode_t ret = replier->take_request(&request, info);
            if (ret == fdds::RETCODE_OK)
            {
                client_id = info.sample_identity.writer_guid();
                ret = replier->get_replier_reader()->get_matched_publication_data(pub_data, client_id);
            }
            return ret;
        }

        bool validate_request() const
        {
            size_t n_fields = 0;
            n_fields += request.update_configuration.has_value() ? 1 : 0;

            return n_fields == 1U;
        }

        void process_additional_request(
                const std::shared_ptr<RequestContext>& ctx,
                frpc::Replier* replier,
                bool& should_erase)
        {
            should_erase = false;
            if (ctx->info.related_sample_identity == info.related_sample_identity)
            {
                // Pass request to input feed processors
                should_erase = true;
                for (const auto& input_feed : input_feed_processors_)
                {
                    if (input_feed->process_additional_request(ctx->request))
                    {
                        should_erase = false;
                        break;
                    }
                }

                // If no input feed processor handled the request, send an exception
                if (should_erase)
                {
                    send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier);
                }
            }
            else
            {
                // This is not the expected request
                should_erase = true;
            }
        }

        bool prepare(
                frpc::Replier* replier)
        {
            if (request.update_configuration.has_value())
            {
                return prepare_update_configuration(replier);
            }


            send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_OPERATION, replier);
            return false;
        }

        void send_exception(
                frpc::RemoteExceptionCode_t ex,
                frpc::Replier* replier)
        {
            ReplyType reply{};
            reply.remoteEx = ex;
            replier->send_reply(&reply, info);
        }

        void cancel()
        {
            // Cancel output feed
            if (output_feed_cancellator_)
            {
                output_feed_cancellator_->cancel();
            }

            // Cancel input feeds
            for (const auto& input_feed : input_feed_processors_)
            {
                input_feed->cancel_input_feed();
            }
        }

    private:

        std::shared_ptr<IOutputFeedCancellator> output_feed_cancellator_;
        std::vector<std::shared_ptr<IInputFeedProcessor>> input_feed_processors_;

        bool prepare_update_configuration(
                frpc::Replier* replier)
        {
            static_cast<void>(replier);
            return true;
        }

    };

    struct ThreadPool
        : public frpc::RpcServerSchedulingStrategy
    {
        ThreadPool(
                HWResourcesServiceServerLogic& server,
                size_t num_threads)
            : server_(server)
        {
            // Create worker threads (at least one)
            if (num_threads == 0)
            {
                num_threads = 1;
            }

            auto process_requests = [this]()
                    {
                        while (!finished_)
                        {
                            std::shared_ptr<frpc::RpcRequest> req;
                            {
                                std::unique_lock<std::mutex> lock(mtx_);
                                cv_.wait(lock, [this]()
                                        {
                                            return finished_ || !requests_.empty();
                                        });
                                if (finished_)
                                {
                                    break;
                                }
                                req = requests_.front();
                                requests_.pop();
                            }

                            // Process the request
                            server_.execute_request(req);
                        }
                    };

            for (size_t i = 0; i < num_threads; ++i)
            {
                threads_.emplace_back(process_requests);
            }
        }

        void schedule_request(
                const std::shared_ptr<frpc::RpcRequest>& req,
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            std::lock_guard<std::mutex> lock(mtx_);
            if (!finished_)
            {
                requests_.push(req);
                cv_.notify_one();
            }
        }

        void server_stopped(
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            // Notify all threads in the pool to stop
            {
                std::lock_guard<std::mutex> lock(mtx_);
                finished_ = true;
                cv_.notify_all();
            }

            // Wait for all threads to finish
            for (auto& thread : threads_)
            {
                if (thread.joinable())
                {
                    thread.join();
                }
            }
            threads_.clear();
        }

        HWResourcesServiceServerLogic& server_;
        std::mutex mtx_;
        std::condition_variable cv_;
        std::queue<std::shared_ptr<frpc::RpcRequest>> requests_;
        bool finished_{ false };
        std::vector<std::thread> threads_;
    };

    void process_request(
            const std::shared_ptr<RequestContext>& ctx)
    {
        auto id = ctx->info.related_sample_identity;

        {
            std::lock_guard<std::mutex> _(mtx_);
            auto it = processing_requests_.find(id);
            if (it != processing_requests_.end())
            {
                bool should_erase = false;
                it->second->process_additional_request(ctx, replier_, should_erase);
                if (should_erase)
                {
                    processing_requests_.erase(it);
                }
                return;
            }

            if (!ctx->prepare(replier_))
            {
                return;
            }

            processing_requests_[id] = ctx;
        }

        request_scheduler_->schedule_request(ctx, shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<RequestContext>& req)
    {
        try
        {
            for (;;)
            {
                if (req->request.update_configuration.has_value())
                {
                    try
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::HWResourcesService_update_configuration_Result{};
                        reply.update_configuration->result = detail::HWResourcesService_update_configuration_Out{};
                        reply.update_configuration->result->return_ = implementation_->update_configuration(
                            *req,
                            req->request.update_configuration->configuration);
                        replier_->send_reply(&reply, req->info);
                    }
                    catch (const InternalError& ex)
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::HWResourcesService_update_configuration_Result{};
                        reply.update_configuration->InternalError_ex = ex;
                        replier_->send_reply(&reply, req->info);
                    }
                    break;
                }

                req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNSUPPORTED, replier_);
                break;
            }
        }
        catch (const frpc::RpcRemoteException& ex)
        {
            req->send_exception(ex.code(), replier_);
        }
        catch (...)
        {
            req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_EXCEPTION, replier_);
        }

        {
            std::lock_guard<std::mutex> _(mtx_);
            processing_requests_.erase(req->info.related_sample_identity);
        }
    }

    fdds::DomainParticipant& participant_;
    frpc::Service* service_ = nullptr;
    frpc::Replier* replier_ = nullptr;
    fdds::GuardCondition finish_condition_;
    std::mutex mtx_;
    std::map<frtps::SampleIdentity, std::shared_ptr<RequestContext>> processing_requests_;
    std::shared_ptr<frpc::RpcServerSchedulingStrategy> request_scheduler_;
    std::shared_ptr<HWResourcesServiceServer_IServerImplementation> implementation_;

};

struct HWResourcesServiceServerProxy
    : public frpc::RpcServer
{
    HWResourcesServiceServerProxy(
            std::shared_ptr<frpc::RpcServer> impl)
        : impl_(std::move(impl))
    {
    }

    ~HWResourcesServiceServerProxy() override
    {
        if (impl_)
        {
            impl_->stop();
        }
    }

    void run() override
    {
        impl_->run();
    }

    void stop() override
    {
        impl_->stop();
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        impl_->execute_request(request);
    }

private:

    std::shared_ptr<frpc::RpcServer> impl_;
};

}  // namespace detail

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_HWResourcesServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        size_t thread_pool_size,
        std::shared_ptr<HWResourcesServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::HWResourcesServiceServerLogic>(
        part, service_name, qos, thread_pool_size, implementation);
    return std::make_shared<detail::HWResourcesServiceServerProxy>(ptr);
}

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_HWResourcesServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServerSchedulingStrategy> scheduler,
        std::shared_ptr<HWResourcesServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::HWResourcesServiceServerLogic>(
        part, service_name, qos, scheduler, implementation);
    return std::make_shared<detail::HWResourcesServiceServerProxy>(ptr);
}

//} interface HWResourcesService

//{ interface CarbonFootprintService

namespace detail {

namespace fdds = eprosima::fastdds::dds;
namespace frpc = eprosima::fastdds::dds::rpc;
namespace frtps = eprosima::fastdds::rtps;

class CarbonFootprintServiceServerLogic
    : public frpc::RpcServer
    , public std::enable_shared_from_this<CarbonFootprintServiceServerLogic>
{
    using RequestType = CarbonFootprintService_Request;
    using ReplyType = CarbonFootprintService_Reply;

public:

    CarbonFootprintServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            size_t thread_pool_size,
            std::shared_ptr<CarbonFootprintServiceServer_IServerImplementation> implementation)
        : CarbonFootprintServiceServerLogic(
            part,
            service_name,
            qos,
            std::make_shared<ThreadPool>(*this, thread_pool_size),
            std::move(implementation))
    {
    }

    CarbonFootprintServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            std::shared_ptr<frpc::RpcServerSchedulingStrategy> scheduler,
            std::shared_ptr<CarbonFootprintServiceServer_IServerImplementation> implementation)
        : frpc::RpcServer()
        , participant_(part)
        , request_scheduler_(scheduler)
        , implementation_(std::move(implementation))
    {
        // Register the service type support
        auto service_type = create_CarbonFootprintService_service_type_support();
        auto ret = service_type.register_service_type(&participant_, "CarbonFootprintService");
        if (ret != fdds::RETCODE_OK)
        {
            throw std::runtime_error("Error registering service type");
        }

        // Create the service
        service_ = participant_.create_service(service_name, "CarbonFootprintService");
        if (nullptr == service_)
        {
            throw std::runtime_error("Error creating service");
        }

        // Create the replier
        replier_ = participant_.create_service_replier(service_, qos);
        if (nullptr == replier_)
        {
            throw std::runtime_error("Error creating requester");
        }
    }

    ~CarbonFootprintServiceServerLogic() override
    {
        if (nullptr != replier_)
        {
            participant_.delete_service_replier(service_->get_service_name(), replier_);
        }

        if (nullptr != service_)
        {
            participant_.delete_service(service_);
        }
    }

    void run() override
    {
        finish_condition_.set_trigger_value(false);
        fdds::WaitSet waitset;
        waitset.attach_condition(finish_condition_);
        fdds::StatusCondition& status_condition = replier_->get_replier_reader()->get_statuscondition();
        status_condition.set_enabled_statuses(fdds::StatusMask::data_available());
        waitset.attach_condition(status_condition);

        while (true)
        {
            fdds::ConditionSeq active_conditions;
            waitset.wait(active_conditions, fdds::c_TimeInfinite);

            // Early exit if the finish condition is triggered
            if (finish_condition_.get_trigger_value())
            {
                break;
            }

            // Take and process the requests
            auto ctx = std::make_shared<RequestContext>();
            while (fdds::RETCODE_OK == ctx->take_from(replier_))
            {
                if (ctx->validate_request())
                {
                    process_request(ctx);
                }
                else
                {
                    ctx->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier_);
                }

                // Prepare next request context
                ctx = std::make_shared<RequestContext>();
            }
        }
    }

    void stop() override
    {
        // Notify all threads to finish
        finish_condition_.set_trigger_value(true);

        // Cancel all pending requests
        {
            std::lock_guard<std::mutex> _(mtx_);
            for (auto& it : processing_requests_)
            {
                it.second->cancel();
            }
            processing_requests_.clear();
        }

        // Wait for all threads to finish
        request_scheduler_->server_stopped(shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        auto ctx = std::dynamic_pointer_cast<RequestContext>(request);
        if (ctx)
        {
            execute_request(ctx);
        }
        else
        {
            throw std::runtime_error("Invalid request context type");
        }
    }

private:

    //{ Output feed helpers

    struct IOutputFeedCancellator
    {
        virtual ~IOutputFeedCancellator() = default;
        virtual void cancel() = 0;
    };

    //} Output feed helpers

    //{ Input feed helpers

    struct IInputFeedProcessor
    {
        virtual ~IInputFeedProcessor() = default;
        virtual bool process_additional_request(
                const RequestType& request) = 0;
        virtual void cancel_input_feed() = 0;
    };

    //} Input feed helpers

    //{ operation update_configuration

    //} operation update_configuration

    struct RequestContext : frpc::RpcRequest
    {
        RequestType request;
        frpc::RequestInfo info;
        frtps::GUID_t client_id;
        fdds::PublicationBuiltinTopicData pub_data;

        struct update_configuration_feeds_t
        {
        }
        update_configuration_feeds;

        const frtps::GUID_t& get_client_id() const override
        {
            return client_id;
        }

        const frtps::RemoteLocatorList& get_client_locators() const override
        {
            return pub_data.remote_locators;
        }

        fdds::ReturnCode_t take_from(
                frpc::Replier* replier)
        {
            fdds::ReturnCode_t ret = replier->take_request(&request, info);
            if (ret == fdds::RETCODE_OK)
            {
                client_id = info.sample_identity.writer_guid();
                ret = replier->get_replier_reader()->get_matched_publication_data(pub_data, client_id);
            }
            return ret;
        }

        bool validate_request() const
        {
            size_t n_fields = 0;
            n_fields += request.update_configuration.has_value() ? 1 : 0;

            return n_fields == 1U;
        }

        void process_additional_request(
                const std::shared_ptr<RequestContext>& ctx,
                frpc::Replier* replier,
                bool& should_erase)
        {
            should_erase = false;
            if (ctx->info.related_sample_identity == info.related_sample_identity)
            {
                // Pass request to input feed processors
                should_erase = true;
                for (const auto& input_feed : input_feed_processors_)
                {
                    if (input_feed->process_additional_request(ctx->request))
                    {
                        should_erase = false;
                        break;
                    }
                }

                // If no input feed processor handled the request, send an exception
                if (should_erase)
                {
                    send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier);
                }
            }
            else
            {
                // This is not the expected request
                should_erase = true;
            }
        }

        bool prepare(
                frpc::Replier* replier)
        {
            if (request.update_configuration.has_value())
            {
                return prepare_update_configuration(replier);
            }


            send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_OPERATION, replier);
            return false;
        }

        void send_exception(
                frpc::RemoteExceptionCode_t ex,
                frpc::Replier* replier)
        {
            ReplyType reply{};
            reply.remoteEx = ex;
            replier->send_reply(&reply, info);
        }

        void cancel()
        {
            // Cancel output feed
            if (output_feed_cancellator_)
            {
                output_feed_cancellator_->cancel();
            }

            // Cancel input feeds
            for (const auto& input_feed : input_feed_processors_)
            {
                input_feed->cancel_input_feed();
            }
        }

    private:

        std::shared_ptr<IOutputFeedCancellator> output_feed_cancellator_;
        std::vector<std::shared_ptr<IInputFeedProcessor>> input_feed_processors_;

        bool prepare_update_configuration(
                frpc::Replier* replier)
        {
            static_cast<void>(replier);
            return true;
        }

    };

    struct ThreadPool
        : public frpc::RpcServerSchedulingStrategy
    {
        ThreadPool(
                CarbonFootprintServiceServerLogic& server,
                size_t num_threads)
            : server_(server)
        {
            // Create worker threads (at least one)
            if (num_threads == 0)
            {
                num_threads = 1;
            }

            auto process_requests = [this]()
                    {
                        while (!finished_)
                        {
                            std::shared_ptr<frpc::RpcRequest> req;
                            {
                                std::unique_lock<std::mutex> lock(mtx_);
                                cv_.wait(lock, [this]()
                                        {
                                            return finished_ || !requests_.empty();
                                        });
                                if (finished_)
                                {
                                    break;
                                }
                                req = requests_.front();
                                requests_.pop();
                            }

                            // Process the request
                            server_.execute_request(req);
                        }
                    };

            for (size_t i = 0; i < num_threads; ++i)
            {
                threads_.emplace_back(process_requests);
            }
        }

        void schedule_request(
                const std::shared_ptr<frpc::RpcRequest>& req,
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            std::lock_guard<std::mutex> lock(mtx_);
            if (!finished_)
            {
                requests_.push(req);
                cv_.notify_one();
            }
        }

        void server_stopped(
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            // Notify all threads in the pool to stop
            {
                std::lock_guard<std::mutex> lock(mtx_);
                finished_ = true;
                cv_.notify_all();
            }

            // Wait for all threads to finish
            for (auto& thread : threads_)
            {
                if (thread.joinable())
                {
                    thread.join();
                }
            }
            threads_.clear();
        }

        CarbonFootprintServiceServerLogic& server_;
        std::mutex mtx_;
        std::condition_variable cv_;
        std::queue<std::shared_ptr<frpc::RpcRequest>> requests_;
        bool finished_{ false };
        std::vector<std::thread> threads_;
    };

    void process_request(
            const std::shared_ptr<RequestContext>& ctx)
    {
        auto id = ctx->info.related_sample_identity;

        {
            std::lock_guard<std::mutex> _(mtx_);
            auto it = processing_requests_.find(id);
            if (it != processing_requests_.end())
            {
                bool should_erase = false;
                it->second->process_additional_request(ctx, replier_, should_erase);
                if (should_erase)
                {
                    processing_requests_.erase(it);
                }
                return;
            }

            if (!ctx->prepare(replier_))
            {
                return;
            }

            processing_requests_[id] = ctx;
        }

        request_scheduler_->schedule_request(ctx, shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<RequestContext>& req)
    {
        try
        {
            for (;;)
            {
                if (req->request.update_configuration.has_value())
                {
                    try
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::CarbonFootprintService_update_configuration_Result{};
                        reply.update_configuration->result = detail::CarbonFootprintService_update_configuration_Out{};
                        reply.update_configuration->result->return_ = implementation_->update_configuration(
                            *req,
                            req->request.update_configuration->configuration);
                        replier_->send_reply(&reply, req->info);
                    }
                    catch (const InternalError& ex)
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::CarbonFootprintService_update_configuration_Result{};
                        reply.update_configuration->InternalError_ex = ex;
                        replier_->send_reply(&reply, req->info);
                    }
                    break;
                }

                req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNSUPPORTED, replier_);
                break;
            }
        }
        catch (const frpc::RpcRemoteException& ex)
        {
            req->send_exception(ex.code(), replier_);
        }
        catch (...)
        {
            req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_EXCEPTION, replier_);
        }

        {
            std::lock_guard<std::mutex> _(mtx_);
            processing_requests_.erase(req->info.related_sample_identity);
        }
    }

    fdds::DomainParticipant& participant_;
    frpc::Service* service_ = nullptr;
    frpc::Replier* replier_ = nullptr;
    fdds::GuardCondition finish_condition_;
    std::mutex mtx_;
    std::map<frtps::SampleIdentity, std::shared_ptr<RequestContext>> processing_requests_;
    std::shared_ptr<frpc::RpcServerSchedulingStrategy> request_scheduler_;
    std::shared_ptr<CarbonFootprintServiceServer_IServerImplementation> implementation_;

};

struct CarbonFootprintServiceServerProxy
    : public frpc::RpcServer
{
    CarbonFootprintServiceServerProxy(
            std::shared_ptr<frpc::RpcServer> impl)
        : impl_(std::move(impl))
    {
    }

    ~CarbonFootprintServiceServerProxy() override
    {
        if (impl_)
        {
            impl_->stop();
        }
    }

    void run() override
    {
        impl_->run();
    }

    void stop() override
    {
        impl_->stop();
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        impl_->execute_request(request);
    }

private:

    std::shared_ptr<frpc::RpcServer> impl_;
};

}  // namespace detail

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_CarbonFootprintServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        size_t thread_pool_size,
        std::shared_ptr<CarbonFootprintServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::CarbonFootprintServiceServerLogic>(
        part, service_name, qos, thread_pool_size, implementation);
    return std::make_shared<detail::CarbonFootprintServiceServerProxy>(ptr);
}

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_CarbonFootprintServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServerSchedulingStrategy> scheduler,
        std::shared_ptr<CarbonFootprintServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::CarbonFootprintServiceServerLogic>(
        part, service_name, qos, scheduler, implementation);
    return std::make_shared<detail::CarbonFootprintServiceServerProxy>(ptr);
}

//} interface CarbonFootprintService

//{ interface MLModelMetadataService

namespace detail {

namespace fdds = eprosima::fastdds::dds;
namespace frpc = eprosima::fastdds::dds::rpc;
namespace frtps = eprosima::fastdds::rtps;

class MLModelMetadataServiceServerLogic
    : public frpc::RpcServer
    , public std::enable_shared_from_this<MLModelMetadataServiceServerLogic>
{
    using RequestType = MLModelMetadataService_Request;
    using ReplyType = MLModelMetadataService_Reply;

public:

    MLModelMetadataServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            size_t thread_pool_size,
            std::shared_ptr<MLModelMetadataServiceServer_IServerImplementation> implementation)
        : MLModelMetadataServiceServerLogic(
            part,
            service_name,
            qos,
            std::make_shared<ThreadPool>(*this, thread_pool_size),
            std::move(implementation))
    {
    }

    MLModelMetadataServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            std::shared_ptr<frpc::RpcServerSchedulingStrategy> scheduler,
            std::shared_ptr<MLModelMetadataServiceServer_IServerImplementation> implementation)
        : frpc::RpcServer()
        , participant_(part)
        , request_scheduler_(scheduler)
        , implementation_(std::move(implementation))
    {
        // Register the service type support
        auto service_type = create_MLModelMetadataService_service_type_support();
        auto ret = service_type.register_service_type(&participant_, "MLModelMetadataService");
        if (ret != fdds::RETCODE_OK)
        {
            throw std::runtime_error("Error registering service type");
        }

        // Create the service
        service_ = participant_.create_service(service_name, "MLModelMetadataService");
        if (nullptr == service_)
        {
            throw std::runtime_error("Error creating service");
        }

        // Create the replier
        replier_ = participant_.create_service_replier(service_, qos);
        if (nullptr == replier_)
        {
            throw std::runtime_error("Error creating requester");
        }
    }

    ~MLModelMetadataServiceServerLogic() override
    {
        if (nullptr != replier_)
        {
            participant_.delete_service_replier(service_->get_service_name(), replier_);
        }

        if (nullptr != service_)
        {
            participant_.delete_service(service_);
        }
    }

    void run() override
    {
        finish_condition_.set_trigger_value(false);
        fdds::WaitSet waitset;
        waitset.attach_condition(finish_condition_);
        fdds::StatusCondition& status_condition = replier_->get_replier_reader()->get_statuscondition();
        status_condition.set_enabled_statuses(fdds::StatusMask::data_available());
        waitset.attach_condition(status_condition);

        while (true)
        {
            fdds::ConditionSeq active_conditions;
            waitset.wait(active_conditions, fdds::c_TimeInfinite);

            // Early exit if the finish condition is triggered
            if (finish_condition_.get_trigger_value())
            {
                break;
            }

            // Take and process the requests
            auto ctx = std::make_shared<RequestContext>();
            while (fdds::RETCODE_OK == ctx->take_from(replier_))
            {
                if (ctx->validate_request())
                {
                    process_request(ctx);
                }
                else
                {
                    ctx->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier_);
                }

                // Prepare next request context
                ctx = std::make_shared<RequestContext>();
            }
        }
    }

    void stop() override
    {
        // Notify all threads to finish
        finish_condition_.set_trigger_value(true);

        // Cancel all pending requests
        {
            std::lock_guard<std::mutex> _(mtx_);
            for (auto& it : processing_requests_)
            {
                it.second->cancel();
            }
            processing_requests_.clear();
        }

        // Wait for all threads to finish
        request_scheduler_->server_stopped(shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        auto ctx = std::dynamic_pointer_cast<RequestContext>(request);
        if (ctx)
        {
            execute_request(ctx);
        }
        else
        {
            throw std::runtime_error("Invalid request context type");
        }
    }

private:

    //{ Output feed helpers

    struct IOutputFeedCancellator
    {
        virtual ~IOutputFeedCancellator() = default;
        virtual void cancel() = 0;
    };

    //} Output feed helpers

    //{ Input feed helpers

    struct IInputFeedProcessor
    {
        virtual ~IInputFeedProcessor() = default;
        virtual bool process_additional_request(
                const RequestType& request) = 0;
        virtual void cancel_input_feed() = 0;
    };

    //} Input feed helpers

    //{ operation update_configuration

    //} operation update_configuration

    struct RequestContext : frpc::RpcRequest
    {
        RequestType request;
        frpc::RequestInfo info;
        frtps::GUID_t client_id;
        fdds::PublicationBuiltinTopicData pub_data;

        struct update_configuration_feeds_t
        {
        }
        update_configuration_feeds;

        const frtps::GUID_t& get_client_id() const override
        {
            return client_id;
        }

        const frtps::RemoteLocatorList& get_client_locators() const override
        {
            return pub_data.remote_locators;
        }

        fdds::ReturnCode_t take_from(
                frpc::Replier* replier)
        {
            fdds::ReturnCode_t ret = replier->take_request(&request, info);
            if (ret == fdds::RETCODE_OK)
            {
                client_id = info.sample_identity.writer_guid();
                ret = replier->get_replier_reader()->get_matched_publication_data(pub_data, client_id);
            }
            return ret;
        }

        bool validate_request() const
        {
            size_t n_fields = 0;
            n_fields += request.update_configuration.has_value() ? 1 : 0;

            return n_fields == 1U;
        }

        void process_additional_request(
                const std::shared_ptr<RequestContext>& ctx,
                frpc::Replier* replier,
                bool& should_erase)
        {
            should_erase = false;
            if (ctx->info.related_sample_identity == info.related_sample_identity)
            {
                // Pass request to input feed processors
                should_erase = true;
                for (const auto& input_feed : input_feed_processors_)
                {
                    if (input_feed->process_additional_request(ctx->request))
                    {
                        should_erase = false;
                        break;
                    }
                }

                // If no input feed processor handled the request, send an exception
                if (should_erase)
                {
                    send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier);
                }
            }
            else
            {
                // This is not the expected request
                should_erase = true;
            }
        }

        bool prepare(
                frpc::Replier* replier)
        {
            if (request.update_configuration.has_value())
            {
                return prepare_update_configuration(replier);
            }


            send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_OPERATION, replier);
            return false;
        }

        void send_exception(
                frpc::RemoteExceptionCode_t ex,
                frpc::Replier* replier)
        {
            ReplyType reply{};
            reply.remoteEx = ex;
            replier->send_reply(&reply, info);
        }

        void cancel()
        {
            // Cancel output feed
            if (output_feed_cancellator_)
            {
                output_feed_cancellator_->cancel();
            }

            // Cancel input feeds
            for (const auto& input_feed : input_feed_processors_)
            {
                input_feed->cancel_input_feed();
            }
        }

    private:

        std::shared_ptr<IOutputFeedCancellator> output_feed_cancellator_;
        std::vector<std::shared_ptr<IInputFeedProcessor>> input_feed_processors_;

        bool prepare_update_configuration(
                frpc::Replier* replier)
        {
            static_cast<void>(replier);
            return true;
        }

    };

    struct ThreadPool
        : public frpc::RpcServerSchedulingStrategy
    {
        ThreadPool(
                MLModelMetadataServiceServerLogic& server,
                size_t num_threads)
            : server_(server)
        {
            // Create worker threads (at least one)
            if (num_threads == 0)
            {
                num_threads = 1;
            }

            auto process_requests = [this]()
                    {
                        while (!finished_)
                        {
                            std::shared_ptr<frpc::RpcRequest> req;
                            {
                                std::unique_lock<std::mutex> lock(mtx_);
                                cv_.wait(lock, [this]()
                                        {
                                            return finished_ || !requests_.empty();
                                        });
                                if (finished_)
                                {
                                    break;
                                }
                                req = requests_.front();
                                requests_.pop();
                            }

                            // Process the request
                            server_.execute_request(req);
                        }
                    };

            for (size_t i = 0; i < num_threads; ++i)
            {
                threads_.emplace_back(process_requests);
            }
        }

        void schedule_request(
                const std::shared_ptr<frpc::RpcRequest>& req,
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            std::lock_guard<std::mutex> lock(mtx_);
            if (!finished_)
            {
                requests_.push(req);
                cv_.notify_one();
            }
        }

        void server_stopped(
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            // Notify all threads in the pool to stop
            {
                std::lock_guard<std::mutex> lock(mtx_);
                finished_ = true;
                cv_.notify_all();
            }

            // Wait for all threads to finish
            for (auto& thread : threads_)
            {
                if (thread.joinable())
                {
                    thread.join();
                }
            }
            threads_.clear();
        }

        MLModelMetadataServiceServerLogic& server_;
        std::mutex mtx_;
        std::condition_variable cv_;
        std::queue<std::shared_ptr<frpc::RpcRequest>> requests_;
        bool finished_{ false };
        std::vector<std::thread> threads_;
    };

    void process_request(
            const std::shared_ptr<RequestContext>& ctx)
    {
        auto id = ctx->info.related_sample_identity;

        {
            std::lock_guard<std::mutex> _(mtx_);
            auto it = processing_requests_.find(id);
            if (it != processing_requests_.end())
            {
                bool should_erase = false;
                it->second->process_additional_request(ctx, replier_, should_erase);
                if (should_erase)
                {
                    processing_requests_.erase(it);
                }
                return;
            }

            if (!ctx->prepare(replier_))
            {
                return;
            }

            processing_requests_[id] = ctx;
        }

        request_scheduler_->schedule_request(ctx, shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<RequestContext>& req)
    {
        try
        {
            for (;;)
            {
                if (req->request.update_configuration.has_value())
                {
                    try
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::MLModelMetadataService_update_configuration_Result{};
                        reply.update_configuration->result = detail::MLModelMetadataService_update_configuration_Out{};
                        reply.update_configuration->result->return_ = implementation_->update_configuration(
                            *req,
                            req->request.update_configuration->configuration);
                        replier_->send_reply(&reply, req->info);
                    }
                    catch (const InternalError& ex)
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::MLModelMetadataService_update_configuration_Result{};
                        reply.update_configuration->InternalError_ex = ex;
                        replier_->send_reply(&reply, req->info);
                    }
                    break;
                }

                req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNSUPPORTED, replier_);
                break;
            }
        }
        catch (const frpc::RpcRemoteException& ex)
        {
            req->send_exception(ex.code(), replier_);
        }
        catch (...)
        {
            req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_EXCEPTION, replier_);
        }

        {
            std::lock_guard<std::mutex> _(mtx_);
            processing_requests_.erase(req->info.related_sample_identity);
        }
    }

    fdds::DomainParticipant& participant_;
    frpc::Service* service_ = nullptr;
    frpc::Replier* replier_ = nullptr;
    fdds::GuardCondition finish_condition_;
    std::mutex mtx_;
    std::map<frtps::SampleIdentity, std::shared_ptr<RequestContext>> processing_requests_;
    std::shared_ptr<frpc::RpcServerSchedulingStrategy> request_scheduler_;
    std::shared_ptr<MLModelMetadataServiceServer_IServerImplementation> implementation_;

};

struct MLModelMetadataServiceServerProxy
    : public frpc::RpcServer
{
    MLModelMetadataServiceServerProxy(
            std::shared_ptr<frpc::RpcServer> impl)
        : impl_(std::move(impl))
    {
    }

    ~MLModelMetadataServiceServerProxy() override
    {
        if (impl_)
        {
            impl_->stop();
        }
    }

    void run() override
    {
        impl_->run();
    }

    void stop() override
    {
        impl_->stop();
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        impl_->execute_request(request);
    }

private:

    std::shared_ptr<frpc::RpcServer> impl_;
};

}  // namespace detail

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_MLModelMetadataServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        size_t thread_pool_size,
        std::shared_ptr<MLModelMetadataServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::MLModelMetadataServiceServerLogic>(
        part, service_name, qos, thread_pool_size, implementation);
    return std::make_shared<detail::MLModelMetadataServiceServerProxy>(ptr);
}

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_MLModelMetadataServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServerSchedulingStrategy> scheduler,
        std::shared_ptr<MLModelMetadataServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::MLModelMetadataServiceServerLogic>(
        part, service_name, qos, scheduler, implementation);
    return std::make_shared<detail::MLModelMetadataServiceServerProxy>(ptr);
}

//} interface MLModelMetadataService

//{ interface MLModelService

namespace detail {

namespace fdds = eprosima::fastdds::dds;
namespace frpc = eprosima::fastdds::dds::rpc;
namespace frtps = eprosima::fastdds::rtps;

class MLModelServiceServerLogic
    : public frpc::RpcServer
    , public std::enable_shared_from_this<MLModelServiceServerLogic>
{
    using RequestType = MLModelService_Request;
    using ReplyType = MLModelService_Reply;

public:

    MLModelServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            size_t thread_pool_size,
            std::shared_ptr<MLModelServiceServer_IServerImplementation> implementation)
        : MLModelServiceServerLogic(
            part,
            service_name,
            qos,
            std::make_shared<ThreadPool>(*this, thread_pool_size),
            std::move(implementation))
    {
    }

    MLModelServiceServerLogic(
            fdds::DomainParticipant& part,
            const char* service_name,
            const fdds::ReplierQos& qos,
            std::shared_ptr<frpc::RpcServerSchedulingStrategy> scheduler,
            std::shared_ptr<MLModelServiceServer_IServerImplementation> implementation)
        : frpc::RpcServer()
        , participant_(part)
        , request_scheduler_(scheduler)
        , implementation_(std::move(implementation))
    {
        // Register the service type support
        auto service_type = create_MLModelService_service_type_support();
        auto ret = service_type.register_service_type(&participant_, "MLModelService");
        if (ret != fdds::RETCODE_OK)
        {
            throw std::runtime_error("Error registering service type");
        }

        // Create the service
        service_ = participant_.create_service(service_name, "MLModelService");
        if (nullptr == service_)
        {
            throw std::runtime_error("Error creating service");
        }

        // Create the replier
        replier_ = participant_.create_service_replier(service_, qos);
        if (nullptr == replier_)
        {
            throw std::runtime_error("Error creating requester");
        }
    }

    ~MLModelServiceServerLogic() override
    {
        if (nullptr != replier_)
        {
            participant_.delete_service_replier(service_->get_service_name(), replier_);
        }

        if (nullptr != service_)
        {
            participant_.delete_service(service_);
        }
    }

    void run() override
    {
        finish_condition_.set_trigger_value(false);
        fdds::WaitSet waitset;
        waitset.attach_condition(finish_condition_);
        fdds::StatusCondition& status_condition = replier_->get_replier_reader()->get_statuscondition();
        status_condition.set_enabled_statuses(fdds::StatusMask::data_available());
        waitset.attach_condition(status_condition);

        while (true)
        {
            fdds::ConditionSeq active_conditions;
            waitset.wait(active_conditions, fdds::c_TimeInfinite);

            // Early exit if the finish condition is triggered
            if (finish_condition_.get_trigger_value())
            {
                break;
            }

            // Take and process the requests
            auto ctx = std::make_shared<RequestContext>();
            while (fdds::RETCODE_OK == ctx->take_from(replier_))
            {
                if (ctx->validate_request())
                {
                    process_request(ctx);
                }
                else
                {
                    ctx->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier_);
                }

                // Prepare next request context
                ctx = std::make_shared<RequestContext>();
            }
        }
    }

    void stop() override
    {
        // Notify all threads to finish
        finish_condition_.set_trigger_value(true);

        // Cancel all pending requests
        {
            std::lock_guard<std::mutex> _(mtx_);
            for (auto& it : processing_requests_)
            {
                it.second->cancel();
            }
            processing_requests_.clear();
        }

        // Wait for all threads to finish
        request_scheduler_->server_stopped(shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        auto ctx = std::dynamic_pointer_cast<RequestContext>(request);
        if (ctx)
        {
            execute_request(ctx);
        }
        else
        {
            throw std::runtime_error("Invalid request context type");
        }
    }

private:

    //{ Output feed helpers

    struct IOutputFeedCancellator
    {
        virtual ~IOutputFeedCancellator() = default;
        virtual void cancel() = 0;
    };

    //} Output feed helpers

    //{ Input feed helpers

    struct IInputFeedProcessor
    {
        virtual ~IInputFeedProcessor() = default;
        virtual bool process_additional_request(
                const RequestType& request) = 0;
        virtual void cancel_input_feed() = 0;
    };

    //} Input feed helpers

    //{ operation update_configuration

    //} operation update_configuration

    struct RequestContext : frpc::RpcRequest
    {
        RequestType request;
        frpc::RequestInfo info;
        frtps::GUID_t client_id;
        fdds::PublicationBuiltinTopicData pub_data;

        struct update_configuration_feeds_t
        {
        }
        update_configuration_feeds;

        const frtps::GUID_t& get_client_id() const override
        {
            return client_id;
        }

        const frtps::RemoteLocatorList& get_client_locators() const override
        {
            return pub_data.remote_locators;
        }

        fdds::ReturnCode_t take_from(
                frpc::Replier* replier)
        {
            fdds::ReturnCode_t ret = replier->take_request(&request, info);
            if (ret == fdds::RETCODE_OK)
            {
                client_id = info.sample_identity.writer_guid();
                ret = replier->get_replier_reader()->get_matched_publication_data(pub_data, client_id);
            }
            return ret;
        }

        bool validate_request() const
        {
            size_t n_fields = 0;
            n_fields += request.update_configuration.has_value() ? 1 : 0;

            return n_fields == 1U;
        }

        void process_additional_request(
                const std::shared_ptr<RequestContext>& ctx,
                frpc::Replier* replier,
                bool& should_erase)
        {
            should_erase = false;
            if (ctx->info.related_sample_identity == info.related_sample_identity)
            {
                // Pass request to input feed processors
                should_erase = true;
                for (const auto& input_feed : input_feed_processors_)
                {
                    if (input_feed->process_additional_request(ctx->request))
                    {
                        should_erase = false;
                        break;
                    }
                }

                // If no input feed processor handled the request, send an exception
                if (should_erase)
                {
                    send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_INVALID_ARGUMENT, replier);
                }
            }
            else
            {
                // This is not the expected request
                should_erase = true;
            }
        }

        bool prepare(
                frpc::Replier* replier)
        {
            if (request.update_configuration.has_value())
            {
                return prepare_update_configuration(replier);
            }


            send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_OPERATION, replier);
            return false;
        }

        void send_exception(
                frpc::RemoteExceptionCode_t ex,
                frpc::Replier* replier)
        {
            ReplyType reply{};
            reply.remoteEx = ex;
            replier->send_reply(&reply, info);
        }

        void cancel()
        {
            // Cancel output feed
            if (output_feed_cancellator_)
            {
                output_feed_cancellator_->cancel();
            }

            // Cancel input feeds
            for (const auto& input_feed : input_feed_processors_)
            {
                input_feed->cancel_input_feed();
            }
        }

    private:

        std::shared_ptr<IOutputFeedCancellator> output_feed_cancellator_;
        std::vector<std::shared_ptr<IInputFeedProcessor>> input_feed_processors_;

        bool prepare_update_configuration(
                frpc::Replier* replier)
        {
            static_cast<void>(replier);
            return true;
        }

    };

    struct ThreadPool
        : public frpc::RpcServerSchedulingStrategy
    {
        ThreadPool(
                MLModelServiceServerLogic& server,
                size_t num_threads)
            : server_(server)
        {
            // Create worker threads (at least one)
            if (num_threads == 0)
            {
                num_threads = 1;
            }

            auto process_requests = [this]()
                    {
                        while (!finished_)
                        {
                            std::shared_ptr<frpc::RpcRequest> req;
                            {
                                std::unique_lock<std::mutex> lock(mtx_);
                                cv_.wait(lock, [this]()
                                        {
                                            return finished_ || !requests_.empty();
                                        });
                                if (finished_)
                                {
                                    break;
                                }
                                req = requests_.front();
                                requests_.pop();
                            }

                            // Process the request
                            server_.execute_request(req);
                        }
                    };

            for (size_t i = 0; i < num_threads; ++i)
            {
                threads_.emplace_back(process_requests);
            }
        }

        void schedule_request(
                const std::shared_ptr<frpc::RpcRequest>& req,
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            std::lock_guard<std::mutex> lock(mtx_);
            if (!finished_)
            {
                requests_.push(req);
                cv_.notify_one();
            }
        }

        void server_stopped(
                const std::shared_ptr<frpc::RpcServer>& server) override
        {
            static_cast<void>(server);

            // Notify all threads in the pool to stop
            {
                std::lock_guard<std::mutex> lock(mtx_);
                finished_ = true;
                cv_.notify_all();
            }

            // Wait for all threads to finish
            for (auto& thread : threads_)
            {
                if (thread.joinable())
                {
                    thread.join();
                }
            }
            threads_.clear();
        }

        MLModelServiceServerLogic& server_;
        std::mutex mtx_;
        std::condition_variable cv_;
        std::queue<std::shared_ptr<frpc::RpcRequest>> requests_;
        bool finished_{ false };
        std::vector<std::thread> threads_;
    };

    void process_request(
            const std::shared_ptr<RequestContext>& ctx)
    {
        auto id = ctx->info.related_sample_identity;

        {
            std::lock_guard<std::mutex> _(mtx_);
            auto it = processing_requests_.find(id);
            if (it != processing_requests_.end())
            {
                bool should_erase = false;
                it->second->process_additional_request(ctx, replier_, should_erase);
                if (should_erase)
                {
                    processing_requests_.erase(it);
                }
                return;
            }

            if (!ctx->prepare(replier_))
            {
                return;
            }

            processing_requests_[id] = ctx;
        }

        request_scheduler_->schedule_request(ctx, shared_from_this());
    }

    void execute_request(
            const std::shared_ptr<RequestContext>& req)
    {
        try
        {
            for (;;)
            {
                if (req->request.update_configuration.has_value())
                {
                    try
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::MLModelService_update_configuration_Result{};
                        reply.update_configuration->result = detail::MLModelService_update_configuration_Out{};
                        reply.update_configuration->result->return_ = implementation_->update_configuration(
                            *req,
                            req->request.update_configuration->configuration);
                        replier_->send_reply(&reply, req->info);
                    }
                    catch (const InternalError& ex)
                    {
                        ReplyType reply{};
                        reply.update_configuration = detail::MLModelService_update_configuration_Result{};
                        reply.update_configuration->InternalError_ex = ex;
                        replier_->send_reply(&reply, req->info);
                    }
                    break;
                }

                req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNSUPPORTED, replier_);
                break;
            }
        }
        catch (const frpc::RpcRemoteException& ex)
        {
            req->send_exception(ex.code(), replier_);
        }
        catch (...)
        {
            req->send_exception(frpc::RemoteExceptionCode_t::REMOTE_EX_UNKNOWN_EXCEPTION, replier_);
        }

        {
            std::lock_guard<std::mutex> _(mtx_);
            processing_requests_.erase(req->info.related_sample_identity);
        }
    }

    fdds::DomainParticipant& participant_;
    frpc::Service* service_ = nullptr;
    frpc::Replier* replier_ = nullptr;
    fdds::GuardCondition finish_condition_;
    std::mutex mtx_;
    std::map<frtps::SampleIdentity, std::shared_ptr<RequestContext>> processing_requests_;
    std::shared_ptr<frpc::RpcServerSchedulingStrategy> request_scheduler_;
    std::shared_ptr<MLModelServiceServer_IServerImplementation> implementation_;

};

struct MLModelServiceServerProxy
    : public frpc::RpcServer
{
    MLModelServiceServerProxy(
            std::shared_ptr<frpc::RpcServer> impl)
        : impl_(std::move(impl))
    {
    }

    ~MLModelServiceServerProxy() override
    {
        if (impl_)
        {
            impl_->stop();
        }
    }

    void run() override
    {
        impl_->run();
    }

    void stop() override
    {
        impl_->stop();
    }

    void execute_request(
            const std::shared_ptr<frpc::RpcRequest>& request) override
    {
        impl_->execute_request(request);
    }

private:

    std::shared_ptr<frpc::RpcServer> impl_;
};

}  // namespace detail

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_MLModelServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        size_t thread_pool_size,
        std::shared_ptr<MLModelServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::MLModelServiceServerLogic>(
        part, service_name, qos, thread_pool_size, implementation);
    return std::make_shared<detail::MLModelServiceServerProxy>(ptr);
}

std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServer> create_MLModelServiceServer(
        eprosima::fastdds::dds::DomainParticipant& part,
        const char* service_name,
        const eprosima::fastdds::dds::ReplierQos& qos,
        std::shared_ptr<eprosima::fastdds::dds::rpc::RpcServerSchedulingStrategy> scheduler,
        std::shared_ptr<MLModelServiceServer_IServerImplementation> implementation)
{
    auto ptr = std::make_shared<detail::MLModelServiceServerLogic>(
        part, service_name, qos, scheduler, implementation);
    return std::make_shared<detail::MLModelServiceServerProxy>(ptr);
}

//} interface MLModelService

